---
interface Props {
  text: string;
  className?: string;
}
const { text, className = "" } = Astro.props;
---

<span class={"badge " + className}>{text}</span>

<style>
  .badge {
    @apply px-2 py-1 text-base font-bold text-white bg-sky-500 rounded-xl;
  }
</style>
