---
import Card from "./Card.astro";

interface Props {
  title: string;
  image?: string;
  alt?: string;
  link: string;
}
const { link, ...rest } = Astro.props;
---

<a href={link} target="_blank"><Card {...rest}><slot /></Card></a>
